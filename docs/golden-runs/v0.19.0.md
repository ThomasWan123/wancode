# WanCode v0.19.0 Release Quality Scorecard

> Candidate commit: `43fb87b80f8ce8fe74a6ae0444b5590d0ff4b40e`  
> Engine commit: `2f480062e2645b0afa760f5b0b552bc4cde5d9e0`  
> Test plan: [`../quality/v0.19-product-test-plan.md`](../quality/v0.19-product-test-plan.md)  
> Exact-SHA CI: [run 31290645637](https://github.com/ThomasWan123/wancode/actions/runs/31290645637)
> Local remediation branch: `fix/v019-hermetic-worktree-snapshot` (`e5dd1cb`, `c66fa31`, `982b84e`, `c825212`, `4fc4449`, `21929f4`, `d7aff80`, `daa4ec5`, `2167d07`, `4203438`)
> Evidence window: 2026-08-09 (in progress)  
> Decision: **NO-GO — evidence collection and defect triage are incomplete**

This scorecard is deliberately conservative. A result is marked PASS only when it
was re-run against the candidate above. Historical v0.18 evidence is not promoted
to v0.19 evidence. Items that have not been exercised remain `NOT-RUN`.

## 1. Executive result

- P0 open: 0 confirmed in the completed deterministic slice.
- P1 open on the candidate: verified test-gate and keyboard-accessibility fixes
  are not yet integrated, and the Windows release artifacts are not signed.
- P2 open: portability, maintainability, and bundle-optimization findings remain;
  the English UI compatibility defect and enforceable bundle gate are fixed on
  the remediation branch.
- Frontend baseline: 55/55 tests passed; typecheck and production build passed.
- Rust library baseline: 175 passed, 1 dedicated probe ignored by design, after
  isolating `GROK_HOME`.
- Effective-tree audit: PASS at the pinned engine commit.
- Isolated real-provider core smoke: PASS, 6/6 scenarios on GLM 5.2.
- Rebuilt desktop candidate: English UI fallback, Chat turn, surface switch,
  command palette, and Escape dismissal passed.
- The latest `4203438` NSIS package completed an install/start/uninstall lifecycle
  with all four user-data roots isolated. A WebView2-isolated zero-model first-run journey also
  passed its non-credential slice. Provider completion, upgrade, rollback, full
  accessibility, signing, and competitive task evidence remain incomplete.

## 2. Automated gates

| Gate | Result | Fresh evidence |
|---|---|---|
| Frontend unit tests | PASS | Vitest: 11 files, 55 tests passed, including legacy-title localization, keyboard activation, new-session workspace retention, shared modal focus contracts, and fail-closed cancellation of backend-blocking dialogs |
| Frontend typecheck | PASS | `npx.cmd tsc --noEmit`, exit 0 |
| Frontend production build | PASS / BUDGET ENFORCED | Latest Vite build passed at 825.99 kB / 239.23 kB gzip. `d7aff80` removes the mixed static/dynamic opener import and adds an 850 KiB per-chunk hard gate. Mutation to 800 KiB failed at the then-measured 824,209 bytes, then the restored gate passed; [evidence](../evidence/v0.19-frontend-bundle-budget.md) |
| Rust Clippy (WanCode) | PASS | `cargo clippy --locked -p wancode --lib --no-deps -- -D warnings`, exit 0 |
| Rust library tests | PASS WITH ISOLATION | 175 passed, 1 ignored; requires an isolated `GROK_HOME` because one test otherwise writes to the real user data root |
| Engine canary | PASS WITH ENVIRONMENT NOTE | 8/8 contracts passed; the commit check required a process-local `safe.directory` because the sandbox user does not own the engine checkout |
| CI combined Rust command | PASS REMOTE / FAIL LOCAL | Clean GitHub runner passed at the exact SHA; the local shared target reproduced output-name collisions and `LNK1104`, so the command remains portability-sensitive |
| Provider contracts 4a/4b | PASS | Transport/error contract and tools/multimodal contract both passed as separate integration targets |
| ACP model block and process breakaway | PASS | Session identity/block contract passed; ordinary descendants died with the Job while breakaway descendants survived as designed |
| Chat extension-lifecycle probe | PASS | Dedicated opt-in probe: Code loaded one plugin and executed its hook; post-broadcast Chat stayed at zero plugins, zero hook executions, and zero local-extension pollution |
| Engine routing, identity, and compaction | PASS REMOTE / TIMEOUT LOCAL | Exact-SHA GitHub Rust job passed all three gates; two isolated local attempts exceeded 600 seconds before tests ran |
| Effective-tree audit | PASS | HEAD, tracked hashes, porcelain set, and effective-tree digest matched lock data |
| Real-provider desktop smoke | PASS | Isolated `GROK_HOME` and workspace; 6/6 passed: create, exact reply, busy queue, interjection, Git stash, and restart/resume |
| Rebuilt desktop candidate GUI | PARTIAL PASS | Exact Chat prompt returned `CHAT-OK`; Chat-to-Code switch created the Code surface; `Ctrl+K` opened the command palette and `Esc` dismissed it; English fallback labels were visually verified |
| Windows bundles | PARTIAL / NO-GO FOR DISTRIBUTION | The latest `4203438` release executable and NSIS installer built successfully and both report `NotSigned`. The same all-bundles run failed in WiX `light.exe`, so the existing MSI is stale relative to the latest remediation head |
| Packaged installer E2E | PARTIAL PASS | The latest `4203438` NSIS package silently installed to an isolated path, launched the installed executable with isolated `APPDATA`, `LOCALAPPDATA`, `GROK_HOME`, and `WEBVIEW2_USER_DATA_FOLDER`, exposed a native main window in 280 ms, then silently uninstalled with exit 0, zero remaining target files, and zero product processes. Upgrade and rollback remain unproven; [evidence](../evidence/v0.19-latest-windows-packaging.md) |
| MSI installer E2E | FAIL TO BUILD LATEST / PRIOR PRIVILEGE BLOCK | The latest all-bundles build compiled the release application but WiX `light.exe` failed without surfacing child stderr. A prior remediation MSI had internally consistent metadata (`ProductVersion=0.19.0`, `ALLUSERS=1`) but silent installation was rejected with error 1925 because the runner lacked an administrator token. The latest head therefore has no valid MSI candidate |
| Fresh-profile first run | PARTIAL PASS | With isolated `APPDATA`, `LOCALAPPDATA`, `GROK_HOME`, and `WEBVIEW2_USER_DATA_FOLDER`, the app showed the localized welcome wizard, empty history, no retained workspace, provider choices, custom endpoint, and deferral path. Deferral intentionally reappears after restart while zero models exist. Credential submission and successful provider setup were not exercised; [evidence](../evidence/v0.19-first-run.md) |
| Update E2E | NOT-RUN | |
| Performance regression | PARTIAL PASS | Three debug-candidate launches exposed a main window in 1,966 / 125 / 125 ms; sampled mean parent working set was 88.2 MB. This is a warm/debug baseline, not a signed-release claim; [evidence](../evidence/v0.19-desktop-performance.md) |
| Accessibility smoke | PARTIAL PASS / FIX VERIFIED | Static and real-keyboard audits found mouse-only custom rows and overlays that lacked dialog/focus isolation. `4fc4449` fixes row semantics; `2167d07` fixes onboarding; `4203438` gives every remaining overlay a shared named-modal, focus-loop, focus-return, and Escape contract. Trust/question/plan Escape paths are tested through their real reject/cancel callbacks. Rebuilt Settings exposed a dialog and passed both focus-loop directions, Escape, and trigger-focus restoration. Screen-reader speech, IME, and scale testing remain open; [onboarding evidence](../evidence/v0.19-onboarding-accessibility.md), [modal evidence](../evidence/v0.19-modal-accessibility.md) |

## 3. Confirmed defects and findings

| ID | Severity | Summary | Reproduction | Required action |
|---|---|---|---|---|
| QA-019-001 | P1, FIX VERIFIED | `snapshot_writes_patch_for_dirty_tree` is not hermetic and writes snapshots under the process-wide real `GROK_HOME` | Baseline failed against the real root; local fix `e5dd1cb` injects an isolated snapshot directory, the targeted regression passed, and the full library suite passed 175/175 with no `GROK_HOME` override | Integrate the verified fix before release sign-off |
| QA-019-002 | P2 | The current combined Rust command is sensitive to identical `wancode_lib` cdylib/staticlib outputs on a reused Windows target directory | Local exact command emitted output filename collisions and failed with `LNK1104`; the clean exact-SHA GitHub job passed | Split library and integration targets or guarantee unique artifacts; retain a clean-run and reused-target regression check |
| QA-019-003 | P2 PERFORMANCE, PARTIAL FIX VERIFIED | Frontend production bundle remains large; the mixed static/dynamic import and lack of an enforceable budget are fixed | Latest build: 825.99 kB / 239.23 kB gzip. `d7aff80` makes the opener import consistently static and enforces 850 KiB per JS chunk; lowering the budget to 800 KiB made the prior build fail at 824,209 bytes | Keep the hard gate; inspect manual chunks and lazy-load low-frequency surfaces before lowering the budget |
| QA-019-004 | P2 | Pinned engine dependencies emit future-incompatible and unused-code warnings | Four implicit `f64` to `f32` fallback warnings plus several unused imports/assignments | Track in the engine maintenance backlog before the next Rust toolchain upgrade |
| QA-019-005 | P2, FIX VERIFIED | Public README described React 18 while `package.json` uses React 19.1.0 | Static documentation/package comparison | `daa4ec5` corrects the product README to React 19; integrate the remediation branch |
| QA-019-006 | P1 TEST-GATE, FIX VERIFIED | The Windows smoke launcher could fail before the application entered `.setup()` | A duplicate `Path`/`PATH` environment caused `Start-Process` failure; a plain Tauri debug build also waited for a development server instead of embedding the frontend | Local fix `c66fa31` canonicalizes `Path`, adds the standard `custom-protocol` feature, makes startup observable and bounded, and then passed all 6 real-provider scenarios |
| QA-019-007 | P1 RELEASE | The Windows executable, MSI, and NSIS installer are unsigned | `Get-AuthenticodeSignature` returned `NotSigned` for all three rebuilt artifacts | Sign the release executable and installers with the project certificate, then verify trust and publisher identity on clean Windows 10 and 11 machines |
| QA-019-008 | P2, FIX VERIFIED | English mode leaked Chinese provider presets and historic untitled-session placeholders | Real desktop inspection showed `智谱开放平台` and `(未命名会话)` while the language selector was English | `982b84e` Englishizes newly created presets and normalizes historic placeholders; `c825212` closes the Code-sidebar path missed by the first fix; frontend tests and build pass |
| QA-019-009 | P2 UX, FIX VERIFIED | Creating a new Code session appeared to clear the active workspace even though state and send logic retained it | Observed during three independent B02 runs; code inspection showed Composer used `sessionId` rather than `workspace` to decide whether the project was connected | `21929f4` makes the workspace state authoritative and adds positive/negative rendering contracts; current 55/55 frontend tests and the production build pass |
| QA-019-010 | P1 ACCESSIBILITY, FIX VERIFIED | Core custom rows were mouse-only and unreachable through Tab/Enter/Space | File tree, grep insertion, queued-message edit, PR link, Workbench file/open rows, and rewind checkpoints used click handlers on non-focusable elements | `4fc4449` adds button/link semantics, `tabIndex`, Enter/Space activation, visible focus styling, and unit coverage; integrate it and complete screen-reader/modal-focus testing before sign-off |
| QA-019-011 | P1 ACCESSIBILITY, FIX VERIFIED | The first-run welcome overlay was not a semantic dialog and allowed keyboard focus to escape behind the mask | On a clean WebView2 profile, initial focus remained on the webview and the first Tab visibly focused the background Chat surface toggle | `2167d07` adds `role=dialog`, `aria-modal`, a programmatic accessible name, initial focus, bidirectional focus trapping, Escape dismissal, and three tests. A rebuilt desktop verified dialog exposure and the first↔last focus loop |
| QA-019-012 | P1 ACCESSIBILITY, FIX VERIFIED | The remaining modal inventory had no shared semantic, focus isolation, or safe Escape contract | Settings, Git, tasks, command palette, rewind, skill editor, trust, question, and plan overlays used independent plain containers. The three engine-blocking overlays could not prove Escape sent a response | `4203438` introduces one shared accessible modal primitive and migrates all nine overlay types. Safe defaults focus reject/cancel actions; tests prove trust Escape sends `trust:false`, question Escape cancels, and plan Escape requests changes. Rebuilt Settings passed UIA dialog exposure, bidirectional loop, Escape, and focus return |
| QA-019-013 | P1 RELEASE | The latest remediation head cannot produce the configured MSI bundle | Release compilation completed, then Tauri failed while invoking WiX `light.exe`; an NSIS-only build succeeded immediately afterward | Capture WiX child stderr in CI, diagnose the linker input/environment, and require both NSIS and MSI bundles from the exact release SHA before sign-off |

## 4. P0 invariants

| Invariant | Result | Evidence |
|---|---|---|
| Chat has no local extensions for its complete lifetime | PASS (automated lifecycle probe) | Mixed Chat/Code process, active Code plugin, reload broadcast, and post-broadcast Chat turn all passed with `fanout_observed=false` |
| Code retains the expected toolchain | PASS (deterministic layer) | Provider tool contract, canary, and exact-SHA GitHub engine routing gate passed |
| Provider endpoint and credential identity are exact | PASS (deterministic layer) | Library identity tests, ACP block contract, provider contracts, and exact-SHA GitHub identity E2E passed |
| Git/worktree operations never target another repository | PARTIAL | Snapshot behavior passed under isolated `GROK_HOME`; full worktree E2E pending |
| Binding migration never silently reclassifies a session | PASS (unit layer) | Surface and migration-gate library tests passed |
| Approval and blocked-state parsing fail closed | PARTIAL | Frontend blocked-composer tests and Rust unit tests passed; packaged UI pending |
| Upgrade preserves configuration, sessions, and credentials | NOT-RUN | |

## 5. Environment and provider matrix

| Environment | Result |
|---|---|
| Windows 11 source tree | PASS FOR AUTOMATED CORE / PARTIAL OVERALL |
| Windows 11 isolated install lifecycle | PARTIAL PASS — latest NSIS install, four-root-isolated installed-process launch, and uninstall cleanup passed; provider setup, upgrade, and rollback remain open |
| Windows 11 upgrade | NOT-RUN |
| Windows 10 release candidate | NOT-RUN |
| English locale | PARTIAL PASS — settings, Chat, Code, command palette, preset labels, and legacy untitled placeholders inspected; full journey pending |
| Simplified Chinese + IME | NOT-RUN |
| 100% / 150% / 200% display scale | NOT-RUN |
| GLM | PASS — GLM 5.2 real-provider smoke, 6/6 |
| DeepSeek | NOT-RUN |
| Generic OpenAI-compatible endpoint | NOT-RUN |

## 6. Competitive and golden tasks

| Task | Result |
|---|---|
| First run and provider setup | PARTIAL PASS — clean WebView2 profile, zero-model welcome, empty state, and deferral behavior passed; credential submission remains NOT-RUN |
| Chat conversation and image input | PARTIAL PASS — exact text turn passed in the rebuilt desktop candidate; image input remains NOT-RUN |
| Code edit, terminal, and test | PARTIAL PASS — B02 passed 3/3 WanCode runs with a one-line production-only fix and 3/3 tests; [evidence](../evidence/v0.19-b02-wancode.md) |
| Session restore, rewind, and cancellation | NOT-RUN |
| Git review and worktree delivery | NOT-RUN |
| Long task, interjection, and recovery | NOT-RUN |
| Backend create, reply, queue, interject, Git, and restore | PASS — isolated real-provider smoke |
| ChatGPT / Claude Code comparison tasks B01–B10 | BLOCKED / IN PROGRESS — WanCode B02 baseline complete. Claude Code `1.0.120` is locally present but its authentication is invalid; the attempted run used zero API tokens and changed no files. A separate equivalent ChatGPT/Codex coding-agent process could not be launched, so neither competitor is scored |

## 7. Release blockers before GO

1. Integrate the verified QA-019-001, QA-019-003, QA-019-005,
   QA-019-006, QA-019-008, QA-019-009, QA-019-010, QA-019-011,
   and QA-019-012 fixes before release sign-off.
   Resolve or schedule QA-019-002 with a documented reused-target regression
   because the clean CI run currently passes.
2. Sign the Windows executable, MSI, and NSIS installer, then verify publisher
   trust on clean Windows 10 and 11 systems.
3. Fix QA-019-013 and produce both configured Windows installers from the exact
   remediated SHA; preserve WiX child diagnostics on failure.
4. Complete the remaining separately targeted Rust gates on the remediated SHA.
5. Extend the now-passing isolated real-provider smoke from GLM to DeepSeek and
   one generic OpenAI-compatible endpoint.
6. Repeat the now-passing install/start/uninstall lifecycle with signed release
   artifacts, then exercise an isolated fresh user profile, upgrade, rollback, IME,
   accessibility, and display scaling.
7. Run the B01–B10 competitive task set against the same fixtures and scoring rubric.
8. Attach durable logs, screenshots, artifact hashes, and defect links; no
   `NOT-RUN` item may be rewritten as PASS based on historical evidence.

## 8. Artifact integrity (current unsigned remediation candidate)

| Artifact | SHA-256 | Signature |
|---|---|---|
| `wancode.exe` (`4203438`, NSIS bundle-type patch, 98,157,568 bytes) | `FE8A75FDD1DFE505B5001B43AE002890ACE4453787200E0F24560CA8644146AE` | `NotSigned` |
| `wancode_0.19.0_x64-setup.exe` (`4203438`, 24,817,837 bytes) | `CA768DA8414EF065767B46F59F82B7F4CC78B30D4B5F0FD98DEC6CCCF6056334` | `NotSigned` |
| `wancode_0.19.0_x64_en-US.msi` | No current artifact: latest WiX `light.exe` step failed | N/A |

These hashes are evidence for the unsigned remediation candidate only. They are not release
distribution identifiers and must be replaced after signing and final bundling.

## 9. Sign-off

| Role | Decision | Date | Notes |
|---|---|---|---|
| Quality | NO-GO | 2026-08-09 | Deterministic, GLM smoke, desktop core, partial keyboard accessibility, and NSIS install/start/uninstall pass; verified fixes still need integration, artifacts are unsigned, and fresh-profile/upgrade/competitive evidence remains open |
| Engineering | Pending | | |
| Product | Pending | | |
