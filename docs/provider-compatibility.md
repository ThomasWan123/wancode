<!-- GENERATED by scripts/gen_compat_matrix.py — do not edit by hand.
     Sources: docs/evidence/compliance-summary-*.json + docs/provider-compat-sources.json.
     Regenerate: python scripts/gen_compat_matrix.py (CI enforces freshness). -->
# Provider compatibility matrix

“OpenAI-compatible” is split into two evidence classes:

1. **Shared mock contract** — suites 4a/4b exercise WanCode's transport,
   streaming, error, tool-call, and multimodal boundaries against scenario
   fixtures. These results are provider-agnostic and must not be presented as a
   live run against any named provider.
2. **Live provider verification** — a provider row is VERIFIED, PARTIAL, or
   NOT-RUN and links only to committed evidence for that real endpoint.

The generator rejects missing/out-of-tree evidence links and refuses a live
claim without evidence. NOT-RUN is never converted into a pass.

## Shared mock suite `4a-transport-errors`

CI commit `f604792ffbd6c8a6190208b4670cd980ca2d2ae3` · 7 scenarios · evidence: [`docs/evidence/compliance-summary-4a.json`](evidence/compliance-summary-4a.json)

| Scenario | Result |
|---|---|
| Standard | PASS |
| NoDoneMarker | PASS |
| NoUsage | PASS |
| ReasoningContent | PASS |
| Err401 | PASS |
| Err429 | PASS |
| Err500Html | PASS |

## Shared mock suite `4b-tools-multimodal`

CI commit `f604792ffbd6c8a6190208b4670cd980ca2d2ae3` · 4 scenarios · evidence: [`docs/evidence/compliance-summary-4b.json`](evidence/compliance-summary-4b.json)

| Scenario | Result |
|---|---|
| ToolRoundtrip | PASS |
| ParallelToolCalls | PASS |
| TranscribeRoute | PASS |
| InlineRoute | PASS |

## Per-provider evidence status

| Provider | Preset id | Endpoint | Shared mock contract | Live status | Live verification |
|---|---|---|---|---|---|
| Zhipu GLM Coding Plan | `glm-coding` | open.bigmodel.cn/api/coding/paas/v4 | shared 4a/4b only (not provider-specific) | **PARTIAL** | Dogfood/release log records real GLM use and smoke through v0.18.9; provider-isolated 4a/4b execution is NOT-RUN. [dogfood and release log](dogfood-log.md) |
| Zhipu Open Platform (pay-as-you-go) | `glm-open` | open.bigmodel.cn/api/paas/v4 | shared 4a/4b only (not provider-specific) | **NOT-RUN** | Separate live endpoint verification has not been run. |
| DeepSeek | `deepseek` | api.deepseek.com/v1 | shared 4a/4b only (not provider-specific) | **PARTIAL** | The v0.18.9 release gate log records a combined real-API smoke 6/6 using GLM and DeepSeek; provider-isolated 4a/4b execution is NOT-RUN. [v0.18.9 release gate log](dogfood-log.md) |
| Custom OpenAI-compatible endpoint | `custom` | user-supplied | shared 4a/4b only (not provider-specific) | **NOT-RUN** | User-supplied endpoints cannot be verified as one provider class. |
