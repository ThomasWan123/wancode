# WanCode v0.19 latest Windows packaging evidence

Date: 2026-08-09  
Product branch: `fix/v019-hermetic-worktree-snapshot`  
Product head: `4203438`

## Build result

- Frontend production build passed at 825.99 kB / 239.23 kB gzip; the 850 KiB
  per-chunk budget remained enforced.
- Release Rust compilation completed successfully.
- The configured all-bundles build failed in WiX `light.exe`.
- A subsequent NSIS-only build completed successfully.

### MSI diagnosis

- A verbose MSI-only build exposed ICE01–ICE09 failures with WiX error
  `LGHT0217`: the Windows Installer Service could not be accessed. WiX ended
  with Win32 error 0x643 at ICE09.
- The generated `main.wixobj` linked successfully when `light.exe` was invoked
  with `-sval`, isolating the current failure to ICE validation/service access
  rather than the authored linker inputs.
- The no-ICE MSI was diagnostic only, was not installed or distributed, and was
  deleted immediately after the result was recorded.
- Release acceptance still requires a normally ICE-validated MSI on a healthy
  Windows host; bypassing validation is not an acceptable workaround.

## Artifact identity

| Artifact | Bytes | SHA-256 | Authenticode |
|---|---:|---|---|
| `wancode.exe` | 98,157,568 | `FE8A75FDD1DFE505B5001B43AE002890ACE4453787200E0F24560CA8644146AE` | `NotSigned` |
| `wancode_0.19.0_x64-setup.exe` | 24,817,837 | `CA768DA8414EF065767B46F59F82B7F4CC78B30D4B5F0FD98DEC6CCCF6056334` | `NotSigned` |

## NSIS lifecycle

1. Confirmed the isolated install root did not exist and no WanCode process was
   running.
2. Silent installation returned exit code 0 and created exactly
   `wancode.exe` and `uninstall.exe` before first launch.
3. Started the installed executable with isolated `APPDATA`, `LOCALAPPDATA`,
   `GROK_HOME`, and `WEBVIEW2_USER_DATA_FOLDER` roots.
4. The process remained alive and exposed a native main window after 280 ms.
5. The isolated profile contained seven files, proving the installed build ran
   far enough to initialize user state without touching the real profile.
6. Closed the application, removed only the explicitly validated disposable
   profile, and silently uninstalled.
7. Uninstall returned exit code 0; the install root no longer existed and zero
   WanCode processes remained.

## Decision impact

The NSIS package is functionally installable, launchable, and removable under
the tested Windows 11 user context. Distribution remains **NO-GO** because the
artifacts are unsigned, the latest MSI cannot be produced, and clean-machine,
upgrade, rollback, and signed-publisher matrices remain incomplete.
