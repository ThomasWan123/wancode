# v0.20.3 professional QA evidence

Date: 2026-08-22 (Asia/Shanghai)

This release publishes the PDF support and mandatory document-gate work merged in
PR #72 on top of the already-published v0.20.2 baseline. The accepted
packaging limitation remains unchanged: Windows artifacts are not
Authenticode-signed.

## Scope over v0.20.2

| Capability | Change | Evidence |
|---|---|---|
| Text PDF import and parsing | PDFium-backed extraction inside the existing crash-contained worker; one source-addressable block per non-empty page | `pdf_end_to_end_through_worker`, `pdf_import_to_model_context` |
| OCR boundary | Image-only PDFs fail with an explicit user-facing message | `image_only_pdf_fails_with_truthful_ocr_boundary` |
| PDF resource limits | Page count, per-page UTF-16, and total UTF-16 caps fail closed | `pdf_page_count_cap`, `pdf_per_page_text_cap`, `pdf_total_text_cap` |
| Source integrity for PDF and DOCX | Fixed safe staging names; SHA-256 re-checked before every model turn | `tampered_pdf_is_rejected_before_model`, `tampered_staged_doc_is_rejected_before_model` |
| Mandatory real DOCX CI gate | Committed `project-orion.docx.b64`; no environment-variable skip | `real_docx_end_to_end_through_worker`, `ci.yml` |
| Complete document/chaos matrix | 21 mandatory checks, zero failures | CI run `32574985775`: `CONTAINMENT DONE pass=21 fail=0` |
| Packaged PDFium runtime | Pinned DLL and license bundled as Tauri resources | `fetch_pdfium.ps1 -VerifyOnly`, `tauri.conf.json` resources |

## Automated gates (pre-release head `2b2cadb`)

| Gate | Result |
|---|---|
| Mandatory document/containment matrix | 21 passed, 0 failed |
| Full Rust CI command on PR #72 head | library 332/0/1 ignored; engine canary 8/8; model block 1/1; provider 1/1 + 1/1; job breakaway; Work surface engine; containment 21/21 |
| Frontend | 16 files / 85 tests; production build |
| GitHub Actions on merged main | pending on version-bump commit |
| PDFium supply-chain verification | version `153.0.7999.0`, locked SHA-256 `fb898a1f5ace57805834f390407500bdb6ef93eff326a252ad334a8aae809d8e` |
| Windows MSI/NSIS bundles | pending `release-candidate` / local `release.ps1` on version `0.20.3` |
| Updater manifest and signature | pending signed NSIS build |

## Authorization

The user authorized publication of v0.20.3 (含 PDF 版本) after PR #72 merged to
`main` at squash commit `2b2cadb072fedd2a407655fe6b9fbf742a4a636c`, accepting
the documented Authenticode unsigned limitation.

## Final artifacts

| Artifact | Bytes | SHA-256 | Authenticode |
|---|---:|---|---|
| `wancode_0.20.3_x64_en-US.msi` | pending | pending | NotSigned (accepted limitation) |
| `wancode_0.20.3_x64-setup.exe` | pending | pending | NotSigned (accepted limitation) |

Installed executable must report product/file version `0.20.3`.

## Published release verification

Pending publication. After assets upload, record:

- release URL and `pub_date`
- all five asset names (`msi`, `setup.exe`, `setup.exe.sig`, `latest.json`, `latest-gh-proxy.json`)
- isolated v0.20.2 → v0.20.3 updater E2E result
- origin manifest/signature and mirror full-installer hash verification
