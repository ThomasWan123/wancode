# v0.19 Frontend Bundle Budget Evidence

Date: 2026-08-09  
Remediation commit: `d7aff80`  
Branch: `fix/v019-hermetic-worktree-snapshot`

## Result

The production frontend build passes an enforceable per-chunk budget. The prior
mixed static/dynamic import of `@tauri-apps/plugin-opener` is gone.

| Measurement | Result |
|---|---:|
| Main JavaScript chunk | 824,209 bytes (824.21 kB in Vite output) |
| Main JavaScript gzip | 238.53 kB |
| CSS | 52.15 kB / 10.18 kB gzip |
| Enforced maximum per JS chunk | 870,400 bytes (850 KiB) |
| Frontend tests at gate introduction | 47/47 passed |

## Mutation proof

The budget was temporarily lowered from 850 KiB to 800 KiB. The production
build failed with the bundle plugin reporting:

```text
assets/index-RJ8jK3XG.js is 824209 bytes; WanCode's per-chunk budget is 819200 bytes
```

The threshold was then restored to 850 KiB and the production build passed.
This proves the gate is active and cannot be satisfied by a warning-only build.

## Remaining risk

The main chunk is still large and has limited headroom. This evidence closes the
warning consistency and enforcement gaps, not the underlying optimization item.
Manual chunking and lazy loading of low-frequency surfaces remain P2 follow-up
work; future growth above 850 KiB is now release-blocking by construction.

After the complete modal accessibility pass, the latest build measured 825.99
kB / 239.23 kB gzip and still passed the unchanged budget; the suite reached
55/55.
