# WanCode v0.23.4 release-candidate evidence

Status: implementation complete; CI, independent review, and packaged Windows
release-candidate pending.

Base: `main@9a160c1044b28c24c7754769929c5dc346c9852c` (`v0.23.3`)

Scope is limited to BUG-01 and BUG-03 through BUG-06 from PR #88, plus removal
of the stale repository-root updater snapshot. BUG-07 (multi-source updater
orchestration) is explicitly out of scope.

## Product behavior

| Contract | Evidence | Result |
|---|---|---|
| Empty transcript keeps the display control reachable | `Messages.view.test.tsx` — `keeps the display control reachable on an idle empty transcript` | PASS |
| Display menu opens on the checked option | `Messages.view.test.tsx` — `opens the menu with focus on the checked display option` | PASS |
| Clearing a transcript closes a leftover display menu | `Messages.view.test.tsx` — `closes an open display menu when the transcript is cleared` | PASS |
| Chat, Code, and Work keep distinct prompts before session creation | `Composer.blocked.test.tsx` — `uses the %s-specific task prompt before a session exists` | PASS |
| Busy and starting prompts retain priority | `Composer.blocked.test.tsx` — `keeps busy and starting hints above surface-specific prompts` | PASS |
| Chat and Work do not offer repository/test actions | `homeSuggestions.test.ts`; `Home.test.tsx` verifies surface wiring | PASS |
| Code retains repository-aware suggestions | `homeSuggestions.test.ts` — `keeps repository and test actions in Code` | PASS |
| Root updater snapshots cannot be mistaken for release assets | root `latest.json` deleted; `/latest*.json` ignored; release generator remains bundle-scoped | PASS |

## Regression proof

The five fixed branches were temporarily restored together. The focused suite
failed 9 assertions across empty-state visibility, selected-option focus,
clear/restore menu state, pre-session prompts, and surface suggestions. After
restoring the implementation, the same focused suite passed 39/39. These
mutations were local verification only and are not part of the commit history.

## Gates

| Gate | Result |
|---|---|
| `npm test` | PASS — 30 files / 166 tests |
| `npm run build` | PASS — TypeScript + production Vite build, 356 modules |
| `git diff --check` | PASS |
| Five version sources | PASS — all `0.23.4` |
| updater manifest generator contract | PASS |
| effective-tree verify | PASS — engine `9e9adb62d`; tree `2132f98c...` |
| version-only migration audit | PASS — V1 through V7, `0.23.3 -> 0.23.4` |
| Required GitHub CI on exact head | PENDING |
| Independent review on exact head | PENDING |
| Windows MSI/NSIS release-candidate lifecycle | PENDING |
| Authenticode | NOT-RUN / NotSigned (previously accepted limitation; release still requires separate authorization) |

No tag, GitHub Release, updater asset, or merge is authorized by this evidence.
