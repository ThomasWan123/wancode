# WanCode v0.23.6 release-candidate evidence

Status: release-candidate preparation and post-merge verification complete;
independent review and publication remain pending.

Base: `main@540501fa6523f8e96b4de0886f9ec09b883b8e41`

Scope is limited to publishing the already-merged heavy-user session-lifecycle
hotfix from PR #92 as `0.23.6`, plus the lock-only remediation of the newly
published `RUSTSEC-2026-0274`. There are no new product or engine-source
changes in this version branch.

## Included product fix

| Contract | Evidence | Result |
|---|---|---|
| A stale primary turn cannot clear the busy state of a newer turn | `turnLifecycle.test.ts` covers A settling and rejecting while B is current | PASS |
| Backend turn-end notifications cannot independently unlock Send | `agent://turn-end` refreshes task/context state only | PASS |
| Opening or clearing a session invalidates stale turn observers | generation bump and `isCurrent()` guards in the merged hotfix | PASS |

## Version-only proof

| Contract | Result |
|---|---|
| Six version sources | PASS — all changed only from `0.23.5` to `0.23.6` |
| Pinned engine commit | PASS — unchanged at `9e9adb62d3251c5cdafbbfe4dcc17ea83b411910` |
| Wiring patch | PASS — unchanged SHA-256 `9c671c0ae43f2e21ab90ff7b0fbb55d9ae0d4138e7ae747be4ec6b22f89da708` |
| Cargo lock | PASS — WanCode `0.23.5 -> 0.23.6`; `rtrb 0.3.2 -> 0.3.5` only |
| Security advisory | PASS — `RUSTSEC-2026-0274` fixed by the compatible upstream `rtrb 0.3.5` release |
| Registered Cargo lock SHA-256 | PASS — `13d667db3047a5b759ed19ef2cf9e4b46e808d9fb76c69a39efe83ff653f0ebb` |
| Registered effective-tree SHA-256 | PASS — `af1a80124bd286fd35b3402a6a069807a4c96031dddede62d643eea238787aea` |
| Emergency patch | PASS — empty / `none` |

## Gates at candidate preparation time

The table below is the evidence snapshot recorded before the candidate workflows
finished and before PR #96 was merged. `PENDING` entries are preserved as
historical state, not as the current status of `main`.

| Gate | Result |
|---|---|
| `npm test` | PASS — 32 files / 182 tests |
| `npm run build` | PASS — TypeScript + production Vite build, 358 modules |
| `git diff --check` | PASS |
| effective-tree verify | PASS — HEAD, hashes, porcelain set, and tree digest match |
| security-upgrade migration audit | PASS — S1 through S10, one upgrade, no additions/downgrades/removals |
| dependency-advisory gate | PASS — 6 remaining hits declared; no undeclared or stale exemptions |
| Required GitHub CI on exact head | PENDING |
| Migration-audit workflow on exact head | PENDING |
| Independent review on exact head | PENDING |
| Windows MSI/NSIS release-candidate lifecycle | PENDING |
| Authenticode | NOT-RUN / NotSigned (accepted for RC preparation only; release still requires separate authorization) |

At the time of this candidate snapshot, no merge, tag, GitHub Release, updater
asset, or publication was authorized by this evidence.

## Post-merge closure

PR #96 was converted from Draft and squash-merged at `2026-09-01T13:31:47Z` by
Claude Code (CC), acting on an explicit in-session user instruction, through the
shared repository account `ThomasWan123`. It was merged before the Windows
release-candidate workflow completed and without an independent review verdict.

| Contract | Evidence | Result |
|---|---|---|
| Candidate head | `0ac71251f8f77d19efa8d74561608a37b90da023` | RECORDED |
| Squash-merged `main` | `454bc9b21321c7c74871867d20a0896e14f3ffe5` | RECORDED |
| Candidate and merged-main tree identity | both resolve to Git tree `1945c8e0456bf7e500df5a778b46036ded263348` | PASS |
| Candidate required CI | run `33506595652`: frontend, versions, dependency-advisory, and rust | PASS |
| Candidate migration audit | run `33506595656` | PASS |
| Windows MSI/NSIS release-candidate lifecycle | run `33512472283`: bundle identities plus clean MSI and NSIS install/uninstall | PASS |
| Exact merged-main required CI | run `33513936089`: frontend, versions, dependency-advisory, and rust | PASS |
| Independent review | no independent verdict was recorded before merge | NOT-RUN |
| Authenticode | certificate import skipped; binaries remain NotSigned | NOT-RUN / accepted for RC preparation only |

The passing candidate Windows workflow ran against Git tree
`1945c8e0456bf7e500df5a778b46036ded263348`. After this documentation correction
is merged, its binary evidence may be reused only if the post-merge review runs
the following command with `NEW_MAIN_SHA` replaced by the exact new `main` SHA:

```text
git diff --stat --exit-code 454bc9b21321c7c74871867d20a0896e14f3ffe5 NEW_MAIN_SHA -- . ":(exclude)docs/evidence/v0.23.6-release-candidate.md"
```

Exit code 0 with empty output proves that no path other than this non-build
evidence file changed. Exit code 1 means another path changed; any other nonzero
code means the check itself failed. Either nonzero result, or any unexpected
output, invalidates RC reuse and requires a new Windows release-candidate run.
This document does not claim that a second party independently reproduced the
existing workflow, nor does it claim an independent `VERDICT: ACCEPT`.

As of this post-merge closure, no `v0.23.6` tag, GitHub Release, release assets,
or updater manifest have been published. Publication still requires independent
review followed by separate, exact-main-SHA user authorization.
