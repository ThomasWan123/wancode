# v0.23.7 release-candidate evidence

## Scope and identity

Prepared by Codex; independent review belongs to Claude Code (CC).
Release preparation was requested by the user. Merge and publication remain
separate authorization points; this document does not grant either.

Base: `4c0fd5e8a44917f77ea9c713568fcc89a99b169f` (merged #115).
Version candidate: `ee2f5cb8c3096cef66597b7a97cd979f7a396731`.
PR: https://github.com/ThomasWan123/wancode/pull/116

The version candidate changes six files (+9/-9): six application version
locations become 0.23.7, and the engine manifest uses `version-only` audit mode
with updated lock and effective-tree hashes. No application source changes.
This evidence-only follow-up adds this document. PR #111 is not included.

Engine commit: `dc46d96fa0460295cf96db86900530743d2197f5`.
Cargo.lock SHA-256: `f1c0346f53887c9cb38da10c490cc478844517f6d3a0d0de3fb9b6be6985628d`.
Effective-tree SHA-256: `3ee4a052bf7bacb1c26f0fbafa2a6ec277a620b5156d9716fe0908260752e8c6`.

## Historical preparation snapshot (2026-09-11)

These results describe the version candidate above, not an unspecified future
main. Pending entries describe this snapshot only. Consult the linked runs and
the PR's SHA-bound review for subsequent results.

| Check | Observed result | Evidence |
|---|---|---|
| Frontend unit tests | PASS: 35 files / 203 tests | Local `npm test` on version candidate |
| Production frontend | PASS | Local `npm run build` |
| Canonical engine identity | PASS | Fresh isolated tree; `audit_effective_tree.ps1 verify` checked HEAD, hashes, changed paths and full digest |
| Version completeness | PASS | versions plus migration-audit V3/V4 cover all six application version locations |
| CI | frontend, versions, dependency-advisory PASS; rust running | https://github.com/ThomasWan123/wancode/actions/runs/34542073581 |
| Migration audit | PASS | https://github.com/ThomasWan123/wancode/actions/runs/34542073610 |
| Windows MSI/NSIS RC | Bundle job running; clean-install results not yet available | https://github.com/ThomasWan123/wancode/actions/runs/34542074620 |
| Independent review | PRELIMINARY, no final verdict | CC review on #116 |
| Packaged interactive GUI and in-app upgrade | NOT-RUN in this preparation round | Must not infer from unit tests or bundle creation |
| Published updater signature and asset checks | NOT-RUN | Publication has not occurred at this snapshot |

Any evidence-only successor must pass required CI on its own SHA and receive
review bound to that SHA. Windows RC is also re-dispatched on the successor;
the earlier run is not represented as a run on the successor. Final run links
and authorizations are recorded on #116 without claiming future outcomes here.

## Authenticode decision and limitation

The user's recorded 2026-08-11 decision deferred Authenticode for personal use.
Reconsider signing before external promotion, expansion to external user groups,
store distribution, or use in environments requiring strict EDR trust.
The RC runs with `require_authenticode=false`; this does not verify a trusted
Windows publisher. Unsigned installers can produce Windows trust warnings or
be blocked by enterprise policy. This is an explicit limitation, not an omitted
test. Final publication authorization must explicitly address this limitation.

Tauri updater signatures are a separate integrity mechanism, not Authenticode.
Their validity against the final installer and equality between both manifests
and the uploaded signature asset must be verified during publication.

## npm advisory boundary

Local `npm audit` reported four affected development packages: three moderate
and one high (Vitest/mocker, baseline-browser-mapping and browserslist).
`npm audit --omit=dev` reported zero production dependency vulnerabilities.
This does not claim the development toolchain, application, or release is free
of vulnerabilities. Dependency remediation is separate from this version-only
candidate; preserve this distinction in release notes.

## Follow-ups

Work text-budget tracking remains #113; smoke static coverage remains #114 and
the separate #111 PR. Added-file audit admission and engine commit reachability
remain audit-hardening follow-ups. None is silently marked fixed by this release.
