# v0.20.2 professional QA evidence

Date: 2026-08-22 (Asia/Shanghai)

This release candidate closes every remediable finding in the local professional-user report. The accepted packaging limitation remains unchanged: Windows artifacts are not Authenticode-signed.

## Findings closed

| Report finding | Product change | Regression evidence |
|---|---|---|
| Work imported DOCX but did not supply it to the model | Every Work turn now rebuilds bounded context from the durable manifest, verifies the staged SHA-256, parses in the contained worker, rejects malformed blocks, and labels serialized document blocks as untrusted data | `work_context` unit tests; `real_docx_import_to_model_context`; packaged GUI answer with exact source citations |
| Repeated startup could remain on Starting and duplicate bindings | A latest-intent coordinator serializes starts, deduplicates identical intents, drops superseded queued starts, and prevents stale results from updating the UI | `startCoordinator.test.ts` (deduplication, serialization, supersession) |
| Restart lost the Work document roster | Engine session and durable Work workspace pointers are persisted independently; a stale/empty engine session is replaced while retaining the same document workspace; the roster is reloaded from the manifest | `workSessionPersistence.test.ts`; `work_workspace_resume_tests`; packaged close/relaunch test |
| Internal document prompt appeared in the user transcript | Live echoes and replay history now unwrap the backend Work envelope and display only the original user request | `workPromptDisplay.test.ts`; packaged GUI transcript inspection |
| Product claimed PDF understanding before a PDF parser existed | Import picker and backend now accept DOCX only and state that boundary explicitly | `work_import::tests::rejects_unsupported_kind`; Work UI inspection |
| Windows full Rust tests could collide on lib/libtest DLL outputs | Windows desktop crate emits the `rlib` actually consumed by the executable; release checklist pins serial test linking | full Rust lib and exact CI gate below |
| README linked the older stable release | Stable links now point to v0.20.1; the unreleased package version is consistently v0.20.2 | version lock inspection and release build |

## Automated gates

| Gate | Result |
|---|---|
| Frontend unit/component suite | 16 files, 85 passed, 0 failed |
| Frontend production build | passed |
| Rust full library suite | 331 passed, 0 failed, 1 intentionally ignored CI-only probe |
| Exact CI Rust command | library gate plus engine canary 8/8, model block 1/1, provider compliance 1/1 + 1/1, Work surface engine pass, job breakaway pass, containment pass |
| Real DOCX parser and anchors | 40 blocks, 1,249 UTF-16 units, 120/120 anchors reproduced across an independent parse |
| Crash/chaos containment with real DOCX | 13 passed, 0 failed: abort, hang deadline/kill, malformed output, output flood cap, oversized input, missing source, containment-unavailable refusal, worker reap, import-to-context, and staged-file tamper rejection |
| Release packaging | MSI and NSIS bundles produced successfully |
| Vendored lock parity | `vendor/grok-build-Cargo.lock` byte-identical to the engine workspace lock |

## Packaged professional-user simulation

The final NSIS bundle was silently installed into an isolated QA directory and launched as an ordinary desktop application using the existing provider configuration.

1. Work opened in `Connected`, without a stuck `Starting` state.
2. The previously imported `Project-Orion-运营简报.docx` appeared automatically after process restart, including the case where the prior engine session had never persisted a message.
3. A document-grounded question returned budget `¥128,400`, owner `Lin Mei（林梅）`, deadline `2026-09-30`, and overall risk `AMBER`, with block-path citations.
4. The embedded prompt-injection instruction was identified as untrusted document content and was not executed.
5. The final transcript displayed only the user's short question; the internal Work context envelope and JSONL were not exposed in the user bubble.

## Final artifacts

| Artifact | Bytes | SHA-256 | Authenticode |
|---|---:|---|---|
| `wancode_0.20.2_x64_en-US.msi` | 50,298,880 | `705d3cf663cd79391848ea7dd343d13c25203420a01c89af4ef3090f1b8a8d2d` | NotSigned (accepted limitation) |
| `wancode_0.20.2_x64-setup.exe` | 25,430,583 | `9a1fa10163a10f35debb31860ca8eed66cb9534f1222100ec488e7bdd1210b8a` | NotSigned (accepted limitation) |

Installed executable reports product/file version `0.20.2`. Release publication, tags, assets, and updater manifests remain NOT-RUN and require a separate exact-head authorization.
