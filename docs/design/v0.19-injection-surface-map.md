# 注入面定位表（`local_extensions_disabled` 开工前置）

> 状态：**第四版（实现复核落实，白名单 18 产品 + 3 既有测试）**。语义已收敛为：
> **禁用会话只能使用内建工具，所有外部扩展通道归零**。
> 每条给出：来源 → 转换 → 会话落点 → 热更新入口 → 主拦截点 + fail-safe →
> Code=false 保持不变的机制 → 判别测试形状。
>
> 引擎路径均相对 `crates/codegen/xai-grok-shell/src/`（另注明者除外）。

## 0. 首要结论：两个 registry 字段，拦截必须落在**会话本地**那一个

定位中最重要的发现——插件 registry 在引擎里有**两份**：

| 字段 | 位置 | 语义 |
|---|---|---|
| `MvpAgent.plugin_registry_handle` | `agent/mvp_agent/mod.rs:833` | **共享**（`SharedPluginRegistryHandle`），进程级，broadcast 的源 |
| `SessionActor.plugin_registry` | `session/acp_session.rs:934` | **会话本地** `RefCell<Option<…>>`，会话真正消费的那份 |

`SessionActor` 另存 `plugin_registry_handle: Option<…>`（`acp_session.rs:938`）。

**因此拦截的主战场是"会话本地字段永远为 `None`"**，而不是去改共享 registry
——后者是 Code 的正当能力（§1.3 承诺口径：不承诺"进程绝不安装插件"）。
一旦 `SessionActor.plugin_registry` 恒 `None`，下游多条链会**自然归零**
（skills、commands gate、slash 展示），这也解释了为什么零贡献能做到而不必
逐个下游打补丁。

## 0.1 入口最早解析（第二版 P0 时序裁决）

**实测顺序问题**：`resolve_mcp_servers` 发生在 actor 构建**之前**，且它内部
调 `ensure_plugin_registry`（`agent_ops.rs:334`）：

| 入口 | `resolve_mcp_servers` 调用点 | 驻留检查位置 |
|---|---|---|
| `new_session`（`acp_agent.rs:811`） | **:835** | — |
| `load_session`（`acp_agent.rs:1256`） | **:1295** | `sessions.borrow().get(&session_id)` 在 **:1295 之后** |

后果两条：

1. 禁用会话会**先解析并合并 MCP、还顺带初始化了共享 plugin registry**，
   等走到 `agent_ops` 才发现策略——副作用已经发生；
2. 驻留 Load 的冲突比较若排在 :1295 之后，**"零副作用冲突拒绝"不成立**
   （MCP 已重解析）。

**裁决**：策略位在 New/Load 入口**最早**解析——`new_session` 在 :835 之前、
`load_session` 在 :1295 之前，紧跟 `arguments` 可用之后：

```
new_session:  parse(arguments.meta) → [禁用? MCP 归零，跳过 resolve] → …
load_session: parse(arguments.meta) → 驻留冲突比较（早退）→ [禁用? 同上] → …
```

`load_session` 的驻留冲突比较必须**上移到 :1295 之前**，与 D5
`reconnect_client_hooks`（:1709）的先后关系自然满足。

## A. LSP（第二版裁决：**Chat 全面禁用**，不只 plugin 贡献）

| 项 | 内容 |
|---|---|
| 来源 | ①插件 manifest `lsp_config_path` / `inline_lsp_servers`（`plugins/manifest.rs:169`）；②用户 `~/.grok/lsp.json`；③项目 `.grok/lsp.json` |
| 转换 | `agent_ops.rs:3267` 取共享 snapshot → `lsp::config::load_servers_with_plugins_sourced(cwd, plugin_paths, inline, …)`（**同一函数同时加载用户/项目 lsp.json 与插件贡献**）→ `folder_trust::filter_untrusted_project_lsp` |
| 会话落点 | `tool_ctx.lsp_server_names` + **`LspManager` 后台进程**（`agent_ops.rs:3300+`） |
| 初始入口 | 会话构建期 `if lsp_tools_enabled && tool_ctx.lsp.is_none()`（`agent_ops.rs:3265`） |
| 热更新入口 | 无独立 reload；新会话（含 Load 重建）重走该块 |
| 主拦截点 | `agent_ops.rs:3265` 条件加 `&& !local_extensions_disabled` ⇒ **整块跳过**：三个来源一并归零，`lsp_server_names` 为空，**`LspManager` 从不构造** |
| fail-safe | `LspManager` 构造点再加一次断言式早退（防将来有人拆分该条件块） |
| Code=false | 条件为 false 时逐字走原路，三个来源照常加载 |
| 判别测试 | **T13a** plugin manifest LSP；**T13b** `~/.grok/lsp.json`；**T13c** 项目 `.grok/lsp.json`——三者在禁用会话中均**无 LSP 名称、无子进程**；Code 对照三者均正常启动 |

> **裁决依据**：LSP 会启动本地后台进程并读取工作区，与 Chat 的零文件面
> 直接冲突；**是否经插件贡献不改变风险**。拦截必须在 `LspManager` 构造
> **之前**——事后 kill 不算隔离。
>
> 语义影响：本条使接口从"禁本地扩展"扩大为**"禁用会话只能用内建工具"**，
> 已在 §0 与接口草案 §1 同步登记。

## B. Plugin skills

| 项 | 内容 |
|---|---|
| 来源 | 插件目录下 `skills/`（`plugins/discovery.rs:646/703`） |
| 转换 | `list_skills_with_plugins(cwd, &skills_config, plugin_snapshot, compat)` |
| 会话落点 | `tool_bridge().update_skill_baseline(new_skills)` → `apply_skill_update_effects` |
| 初始入口 | 会话构建期的 skills 装配 |
| 热更新入口 | **`reload_skills_from_disk`**（`session_setup.rs:174`），被两处调用：`hooks_plugins.rs:974`（plugin reload 后）与 `run_loop.rs:688`（`SessionCommand::ReloadSkills`） |
| 主拦截点 | **天然归零**：`session_setup.rs:177` 读的是 `self.plugin_registry.borrow().clone()`（**会话本地**字段）。§0 的主拦截让它恒 `None` ⇒ `plugin_snapshot = None` ⇒ 零 plugin skills |
| fail-safe | `reload_skills_from_disk` 开头加显式早退（禁用时直接 `return 0`），防将来有人把数据源改回共享 handle |
| Code=false | 会话本地字段照常被 snapshot 填充，两条调用路径不变 |
| 判别测试 | 插件带 `skills/` → 禁用会话技能列表不含该技能且 `/plugins reload` 后仍不含；Code 对照两者都含 |

## C. Plugin / slash commands

| 项 | 内容 |
|---|---|
| 来源 | 插件 `commands/` 目录（`discovery.rs:646`）+ 内建命令表 |
| 转换 | `slash_commands::available_commands(&skills, availability)`；`all_commands` 按 `availability.allows(gate)` 过滤（`slash_commands.rs:336`） |
| 会话落点 | `AvailableCommandsUpdate` 通知（`session_setup.rs:197 send_available_commands_update`） |
| 初始入口 | `SessionCommand::AdvertiseCommands`，由 `agent_ops.rs:3691`（新建）与 `acp_agent.rs:2129`（Load）发出 |
| 热更新入口 | 同一 `AdvertiseCommands`；另 `model_switch.rs:274` 换模型后重广告；`run_loop.rs:685` 处理 |
| 主拦截点 | **天然归零**：`BuiltinGate::Plugins` 由 `acp_session.rs:1177 plugins: self.plugin_registry.borrow().is_some()` 派生 ⇒ 会话本地恒 `None` ⇒ `/plugins` 等命令不进列表；plugin commands 随 skills 一并消失（同 §B 数据源） |
| fail-safe（**兜底拒绝**） | 命令不在列表 ≠ 不能直接调用——`slash_exec.rs` 的 `/plugins`、`/reload-plugins`、`/hooks` 分支**必须各自显式拒绝**（结构化错误），不能只靠 gate 隐藏 |
| Code=false | gate 照常由本地 registry 派生，命令表不变 |
| 判别测试 | 禁用会话的 `AvailableCommandsUpdate` 不含 `plugins`/`reload-plugins`/`hooks`；**且**直接发送 `/plugins reload` 文本 → 结构化拒绝（T4）；Code 对照两者都可用 |

## D. Hooks 全来源（**五条，缺一即漏**）

| # | 来源 | 落点/入口 | 拦截点 |
|---|---|---|---|
| D1 | **磁盘 hooks**（`~/.grok/hooks`、hooks-paths、Claude/Cursor 全局） | `agent_ops.rs:3469 discover_hooks(...)`（会话构建期） | 禁用时**跳过整个 discover**，注入空 registry |
| D2 | **plugin hooks**（含 inline） | `hooks_plugins.rs:832` 在 reload/apply snapshot 时追加 | §0 主拦截使本地 registry 恒 `None` ⇒ 无可追加；**并在 `apply_plugin_registry_snapshot` 早退**（第二道） |
| D3 | **`AgentDefinition.hooks`** | agent 定义自带（`xai-grok-agent/config.rs` 的 `hooks` 字段），随 profile 进会话 | **裁决：忽略而非拒绝建会话**——禁用策略优先于 profile。实现为「该会话装配时忽略 agent inline hooks + 记诊断日志」，**不修改共享 `AgentDefinition` 对象**（它可能被其他会话复用）。这条**不经 plugin registry**，§0 主拦截覆盖不到（T14 打这条）；`hook_dispatch` 早退为第二道 |
| D4 | **New 的 client hooks** | `agent_ops.rs:3355 parse_client_hooks(session_meta)` → `:3610` 装配 | 禁用时解析结果**丢弃/拒绝**（T10） |
| D5 | **驻留 Load 的 `reconnect_client_hooks`** | `acp_agent.rs:1709` → `:1713 handle.set_client_hooks(hooks)` | 禁用时**不调用**；且此调用发生在 reconnect 副作用中 ⇒ **必须排在 §F 的冲突比较之后** |

Code=false：五条全部走原路。
判别测试：D1/D2 由 T1/T3 覆盖；**D3 = T14**（agent inline hook）；
**D4 = T10**；**D5** 需单独一条：驻留禁用会话被 Load 且 meta 带 client hooks
→ `set_client_hooks` 未被调用（断言 `snapshot_client_hooks()` 仍空）。

## E. MCP 全来源（**四条，引擎侧全部归零**）

> **第二版裁决**：删除"managed MCP 不属于本接口"的表述。理由——Chat/Code
> **共用连接**，而客户端的 managed-MCP 实例级覆盖是**连接/进程级**手段，
> 不构成会话级边界。禁用会话必须在**引擎内**同时归零四条来源。
> 客户端现有四道检查**继续保留**，作为请求构造证据与纵深防御，
> **但不再是唯一防线**。Code 会话照常使用 MCP。

| # | 来源 | 落点/入口 | 引擎侧拦截点 |
|---|---|---|---|
| E1 | **plugin MCP** | `agent_ops.rs:337 merge_managed_mcp_servers(..., plugin_registry_handle.snapshot(), ...)`；reload 时 `hooks_plugins.rs:902` 重新合并并建连 | 禁用时不进入 merge（§0.1 早退，整个 `resolve_mcp_servers` 跳过）；reload 路径由 `apply_..._snapshot` 早退挡住 |
| E2 | **managed MCP** | `agent_ops.rs:335 get_managed_mcp_configs()`；开关 `acp_agent.rs:373` | 禁用时**不调用** `get_managed_mcp_configs`（随 §0.1 整块跳过），与 `cfg.managed_mcps_enabled` 无关 |
| E3 | **请求 `mcp_servers`** | `new_session acp_agent.rs:833/835`、`load_session :1295` | 禁用时**丢弃** `arguments.mcp_servers`，不传入 resolve |
| E4 | **`_meta["x.ai/mcp/servers"]`** | `agent_ops.rs:3523 parse_acp_mcp_servers(...)` | 禁用时**忽略该字段**（meta 通道，第三方客户端也可能带） |

**统一实现形态**：§0.1 的入口早解析后，禁用分支直接以
`(mcp_servers = [], managed_mcp_expires_at = None)` 替代整个
`resolve_mcp_servers` 调用——四条来源一并归零，且**不触发**
`ensure_plugin_registry`。E4 另在 `agent_ops.rs:3523` 单独早退。

Code=false：四条全部走原路。

## F. 驻留 actor：策略位的读取位置与同步判定

**问题**：`extensions/*` 与 broadcast 需要在**不向 actor 发消息**的情况下
同步判定策略位（发消息是异步的，且 broadcast 遍历时不能逐个 await）。

**定位**：`SessionHandle`（`session/handle.rs:41`）是 `MvpAgent` 侧持有的
会话句柄，`session_handle_waiting_for_load`（`agent_ops.rs:1742`）是统一
取用入口，`extensions/plugins.rs:142`、`acp_agent.rs:2187/3277/3322` 都经它。

**方案**：在 `SessionHandle` 上加 `pub local_extensions_disabled: bool`
（**普通 bool，非锁**——构造时写入、之后只读，避免 broadcast 遍历时取锁）。

| 消费方 | 读取方式 |
|---|---|
| `extensions/plugins.rs` / `hooks.rs` / `session_admin.rs` | `session_handle_waiting_for_load(&sid)` → 读字段 → 拒绝或返回空 DTO |
| `broadcast_plugin_registry_to_sessions`（`mvp_agent/mod.rs`） | 遍历 `self.sessions` 时读字段 → 跳过 |
| Load 冲突比较（T8/T9） | `acp_agent.rs` Load 分支**最先**读该字段与请求值比较，**早于** D5 的 `reconnect_client_hooks`（:1709）与其他 reconnect 副作用 |
| 握手 `applied`（T12） | 新建取新 actor 构造结果；Load 取 handle 字段 |

`SessionActor` 侧同名字段（`acp_session.rs`）供 actor 内部逻辑（skills
reload 早退、hook dispatch 早退）使用。**两处必须在同一构造点写入同一值**，
测试断言二者一致（防"handle 说禁用、actor 没禁用"的分裂）。

## I. 子代理派生链（**第三版新增；前两版完全未覆盖**）

> 编号说明：§A–§F 与本节是**注入链**，§G（白名单）/§H（裁决）是元章节，
> 字母已被占用。本节取 `I` 并**排在链的末位、元章节之前**，保持
> "先列全部链、再冻结白名单"的阅读顺序；字母不连续是刻意的。

> 发现经过：实现 §2 的锁存字段时发现 `spawn_session_on_thread` 有**第三个
> 调用方**——`agent/subagent/handle_request.rs:1080`。子代理**不走**
> `spawn_and_register_session`，因此 §A–§F 六条链**一条都拦不到它**。
> 这不是接线问题，是独立的第七条注入链。

| 项 | 内容 |
|---|---|
| 来源 | ①**共享 plugin registry**（`plugin_registry_handle.snapshot()`）；②`AgentDefinition.hooks`（agent 定义 inline hooks）；③`AgentDefinition.mcp_servers`（Named 引用 + **Inline 定义**） |
| 转换 | `subagent_coordinator.rs` 构造 `SubagentValidationContext`(:280) 与 `SubagentSpawnContext`(:502)，**两处都写 `plugin_registry: self.plugin_registry_handle.snapshot()`**；`handle_request.rs:881-925` 把 `AgentDefinition.hooks` 解析为 specs 并 `unwrap_or_default().append_specs(...)` **重建** hook registry；`:927` 把 `AgentDefinition.mcp_servers` 物化为 `agent_mcp_servers` |
| 会话落点 | 子代理 actor（经 `spawn_session_on_thread` → `spawn_session_actor`） |
| 初始入口 | 父会话的 subagent 工具调用 → `handle_request` |
| 热更新入口 | 无（每次派生重新构造） |
| 主拦截点 | `subagent_coordinator.rs`：读**父 `SessionHandle`** 的策略位（同处已有 `.and_then(\|h\| h.hook_registry.clone())`:398-402 的先例），禁用时两个 ctx 的 `plugin_registry` 均置 `None` 并携带策略位；`handle_request.rs`：inline hooks 分支早退、`agent_mcp_servers` 恒空 |
| Code=false | 三条来源逐字走原路 |
| 判别测试 | **T19**（严格继承 + handle/rebuild spec 一致）、**T20**（三条来源同时存在仍全零）、**T21**（父会话驱逐竞态 fail-closed），见接口草案 §7 |

### I.1 三条来源的实证与危害排序

| # | 来源 | 现状 | 危害 |
|---|---|---|---|
| I1 | **共享 plugin registry** | `subagent_coordinator.rs:280` / `:502` 直取 `snapshot()`，**与父会话的本地零值无关** | **最高**——禁用父会话的子代理直接拿到全量插件；父会话的四层拦截全部绕过 |
| I2 | `AgentDefinition.hooks` | `handle_request.rs:919-922` `unwrap_or_default()` 后 `append_specs`——父 registry 为 `None` 时**凭空造一个非空 registry** | 高——D3 在子代理里原样复活 |
| I3 | `AgentDefinition.mcp_servers` | `:927` Named 引用需命中 `ctx.parent_mcp_configs`（禁用父会话为空，天然失效），但 **`McpServerRef::Inline` 直接从 definition 物化**，不依赖父会话 | 中——仅 Inline 形态可绕过 |

现有闸门只有 `is_plugin_agent` 与 `folder_trust::agent_inline_hooks_allowed`
两道，**都不看会话策略位**。

### I.2 已排除 / 未排除的相邻面（第三版修订：上一版把 I3 漏进了"已排除"）

**已排除——broadcast 与扩展端点够不着子代理**：`self.sessions` 的
`insert` 只发生在 `agent_ops.rs:3741`（`spawn_and_register_session` 内），
子代理不经此路，故 `broadcast_plugin_registry_to_sessions` 与所有按
`session_id` 查 `self.sessions` 的扩展端点都触及不到子代理会话。

**未排除——按面分类，三条来源各自独立**：

| 面 | 结论 |
|---|---|
| **hook / plugin registry 面** | 问题是 **I1 与 I2**：I1 另取共享源（`snapshot()`），I2 在父值为 `None` 时**重建**非空 registry |
| **MCP 面** | 问题是 **I3，独立绕过面**。上一版把它归进"已排除"是错的——Named 引用确实因父 MCP 为空而天然失效，但 `McpServerRef::Inline` **直接从 `AgentDefinition` 物化**，不读父会话任何状态，与 I1/I2 无关，必须单独归零 |
| `client_hooks` | **不是"天然继承"**（上一版的简写不准）：`SubagentSpawnContext` 构造时是 `client_hooks: Default::default()`（`subagent_coordinator.rs:441`），**随后**在 drain 臂里异步填入——`ctx.client_hooks = handle.snapshot_client_hooks().await`（`:40`）。即"先默认空、再异步读父 handle 覆盖"。父会话为禁用态时最终值为空是**正确结果**，但它依赖那次异步读取真的发生；父会话缺失时停留在 `Default`（空）**是 fail-closed 的巧合，不是设计**。实现时应显式处置，不得依赖此巧合 |

### I.3 父会话缺失：validation context 必须 fail-closed

`build_subagent_validation_context`（`subagent_coordinator.rs:259`）的
doc comment 明写 **"tolerates evicted parent sessions (returns built-in
defaults + warns)"**：`parent_cwd` → `unwrap_or_default()`、
`allowed_subagent_types` → `None`，而 `:280` 的
`plugin_registry: self.plugin_registry_handle.snapshot()` **无条件执行**。

加入策略位后，"父会话被驱逐 ⇒ 读不到策略位 ⇒ 回落 `false` ⇒ 继续交出
共享 registry"是一条**驱逐竞态下的插件 agent 重新暴露路径**。

**裁决**：父会话存在则严格继承；**缺失则 `plugin_registry` 必须为 `None`，
或直接返回 `Unavailable`**——读不到策略位一律按最严处理。判别测试 **T21**
（接口草案 §7 / §2.4）。

## G. 白名单冻结（**第三版重算：17 个产品文件**）

> **机器清单 `docs/design/v0.19-engine-file-whitelist.txt` 是脚本唯一
> 读取源**；本表仅供人读（改动说明与行数预估）。两者不一致时脚本判红。
> 路径省略 `crates/codegen/xai-grok-shell/src/` 前缀。

### G.1 锁存与传播

| # | 文件 | 改动 | 预估 |
|---|---|---|---|
| 1 | `session/handle.rs` | +1 只读 bool（broadcast 遍历与扩展端点的无锁读取源） | ~3 |
| 2 | `session/agent_rebuild.rs` | `AgentRebuildSpec` +1 字段 + 同文件 `#[cfg(test)] test_rebuild_spec_default()` 加默认值 | ~4 |
| 3 | `session/acp_session_impl/spawn.rs` | 参数传递 + **hook registry 归零**（`:1057` `hook_registry_override` 的最终选择/发现；与 `agent_ops.rs:3469` 的 D1 输入端**共同构成防线**，不是二选一）+ handle/rebuild_spec 构造 | ~12 |
| 4 | `session/acp_session.rs` | 能力 gate 经 `rebuild_spec` 读策略位；`plugins`/`hooks` gate 派生处（:1176-1177）注释 | ~5 |

> **存储方案（第三版裁决）**：**不给 `SessionActor` 加字段**。实测
> `SessionActor { … }` 字面量 **25 处、分布在 14 个测试文件**
> （`acp_session_tests/*` 与 `compaction.rs` 内联测试），加字段会造成
> 大面积机械改动、淹没本批的安全 diff。改用既有
> `SessionActor.rebuild_spec: Arc<AgentRebuildSpec>`（`acp_session.rs:1026`）
> 承载：`AgentRebuildSpec` 字面量实测仅 **2 处**——`spawn.rs:820` 与
> `agent_rebuild.rs:375` 的测试 stub（那 14 个测试文件正是共用此 stub），
> **既有测试文件零改动**。附带收益：策略位随会话重建（模型切换等）自动
> 保持，不会因重建丢失。
>
> **`spawn.rs` 的审计约束（第三版更正）**：**不是纯接线**——它同时负责
> hook registry 的选择/发现（`:1057` `hook_registry_override` 分支），
> 禁用态需要在此把 registry 固定为 `None`，因此**允许明确的策略分支**。
> 约束改为：**仅限参数传递、hook registry 归零、actor/handle/rebuild_spec
> 构造**三类；出现其他语义的改动即判红。（前一轮"要求零 `if`"的提法作废。）

### G.2 入口与握手

| # | 文件 | 改动 | 预估 |
|---|---|---|---|
| 5 | `agent/mvp_agent/acp_agent.rs` | 入口最早解析（new:835 前 / load:1295 前）+ 驻留冲突比较上移到 :1295 之前 + MCP 四源归零 + D5 不调用 + **两个响应回显（:1251 / :2125）** | ~58 |
| 6 | `agent/mvp_agent/agent_ops.rs` | session_meta 解析(T11) + 本地 registry 传 `None` + D1 discover 跳过 + D3 忽略 agent hooks + D4 丢弃 client hooks + E4 忽略 meta MCP + LSP 整块跳过 + `ensure_plugin_registry` 调用计数(T16b) | ~72 |
| 7 | `agent/mvp_agent/mod.rs` | `broadcast_plugin_registry_to_sessions`(:2120) 遍历跳过 | ~8 |

> 第三版更正：§G 此前把"两处响应回显"记在 `agent_ops.rs`，实测两个响应
> 都在 `acp_agent.rs` 构造（`:1251` `NewSessionResponse`、`:2125`
> `LoadSessionResponse`）。两文件均在清单内，不越界，仅行数在两者间挪。

### G.3 子代理派生链（§I）

| # | 文件 | 改动 | 预估 |
|---|---|---|---|
| 8 | `agent/mvp_agent/subagent_coordinator.rs` | 读父 `SessionHandle` 策略位；`SubagentValidationContext`(:280) 与 `SubagentSpawnContext`(:502) 的 `plugin_registry` **均置 `None`** 并携带策略位 | ~14 |
| 9 | `agent/subagent/mod.rs` | 两个 ctx 结构体各 +1 字段（`:283` 附近 / `:1623` 附近） | ~6 |
| 10 | `agent/subagent/handle_request.rs` | 继承策略位；I2 inline hooks 分支早退（:881-925）；I3 `agent_mcp_servers` 恒空（:927，含 `Inline`）；转发给 `spawn_session_on_thread`(:1080) | ~16 |

### G.4 会话内第二道防线

| # | 文件 | 改动 | 预估 |
|---|---|---|---|
| 11 | `session/acp_session_impl/hooks_plugins.rs` | `apply_plugin_registry_snapshot` 早退 + reload 拒绝 + MCP 不合并 | ~35 |
| 12 | `session/acp_session_impl/session_setup.rs` | `reload_skills_from_disk` 早退（fail-safe） | ~6 |
| 13 | `session/acp_session_impl/slash_exec.rs` | **完整 `hooks`/`plugins` 命令族（13 个 arm）兜底拒绝**（第四版扩大：`/plugins install\|add\|remove\|update\|trust` 改的是同连接其他会话会读的全局插件状态，只塞只读命令没有意义）；表驱动测试逐项锁定（T23） | ~36 |
| 14 | `session/acp_session_impl/hook_dispatch.rs` | 派发前早退（第二道） | ~6 |

### G.4a 测试通道（仅 feature 门控）

| # | 文件 | 改动 | 预估 |
|---|---|---|---|
| 18 | `session/commands.rs` | `SessionCommand` +1 **feature 门控**变体：只带命令文本与 `oneshot::Sender<()>`，**不含任何策略判断** | ~8 |
| 19 | `session/acp_session_impl/run_loop.rs` | `SessionCommand` 的**唯一真实消费点**；+1 **feature 门控** match arm：用真实解析器（与 `turn.rs` 同一入口，`availability = all_enabled()`）解析后直接调生产 `execute_builtin_slash_command`，返回后回 `()`。**不得含策略判断、不得构造载荷** | ~18 |

> **为何是两个文件**：变体本身无人消费则 `match` 不穷尽、编译即失败，
> `run_loop.rs` 是 `SessionCommand` 的唯一真实消费点。
>
> **拒绝载荷不走这条通道**：`execute_builtin_slash_command` 的返回值不含载荷，
> 载荷经 gateway 通知发出（挂在 `TextContent._meta`）。测试因此仍从既有的
> notification collector 读取——通道只负责「跑完真实 dispatch」，
> **不制造第二套结果表达**，否则断言的就不再是客户端真正看到的东西。
>
> **为何扩容**：`BuiltinAction` 是 `pub(super)`、`SessionActor` 不出 crate，
> 集成测试够不到 dispatch。不开这条通道，就只能在测试里复刻拒绝判定——
> 而那样的测试在守卫被删除、后移或出现第二条调用路径时**仍会全绿**，
> 与本轮消除假强度的纪律相悖。默认 feature 关闭时，变体与入口**完全不存在**。

### G.5 扩展端点

| # | 文件 | 改动 | 预估 |
|---|---|---|---|
| 15 | `extensions/plugins.rs` | action 拒绝 + list 空 DTO | ~18 |
| 16 | `extensions/hooks.rs` | action 拒绝 + list 空 DTO | ~18 |
| 17 | `extensions/session_admin.rs` | `x.ai/plugins/reload` **条件式**（第四版修正，见下） | ~25 |

**产品文件 20 个（含 `Cargo.toml` 的 feature 与 `commands.rs` + `run_loop.rs` 的测试通道），约 374 行。**

> **`x.ai/plugins/reload` 的条件式语义（第四版）**：该端点**没有 session
> id**，是进程级重建，混合连接中的 Code 仍然需要它，故**不整体拒绝**。
> 但也不能无条件执行：
>
> | 连接上的会话 | 处置 |
> |---|---|
> | 至少一个非禁用会话 | **允许重建**；anchor cwd **只能取非禁用会话**（原实现取 `sessions` 的第一个，会把全局项目级插件发现锚到一个根本不许用插件的会话上）。禁用会话由 broadcast 跳过 + snapshot 早退双门隔离 |
> | 只有禁用会话 | **结构化拒绝**（`no_eligible_session`），**不执行重建**——此时没有任何会话能消费结果，照常重建等于告诉调用方「reload 成功了」而实际无人可用 |
>
> 混合连接必须有回归测试（T22）：Code 更新成功，Chat 的
> registry / hook / MCP 仍严格为零。

### G.6 既有测试文件（`[tests]` 段）

| 文件 | 改动 |
|---|---|
| `agent/mvp_agent/tests.rs`（:1131） | `SessionHandle { … }` 字面量 +1 字段 |
| `agent/subagent/tests/mod.rs`（:1080） | 同上 |

实测全仓 `SessionHandle` 字面量仅此两处。新增测试文件（T1–T20，约
560–620 行）不入清单，由 A2 的"新增文件"判定放行。

### G.7 较第二版的变化（如实登记）

- **新增** `spawn.rs`（第二版误判为"仅 hook registry 落点不在此"而移出，
  但它是 `SessionActor`/`SessionHandle`/`AgentRebuildSpec` 的**唯一构造点**，
  且确实承担 hook registry 归零）；
- **新增** `agent_rebuild.rs`（存储方案改用 `AgentRebuildSpec`）；
- **新增** §I 三文件：`subagent_coordinator.rs`、`subagent/mod.rs`、
  `subagent/handle_request.rs`；
- 行数由 ~247 上调至 ~304：§I 链 ~36 行 + 存储/接线 ~21 行。

## H. 三项裁决（已定，第二版落实）

| # | 议题 | 裁决 | 落点 |
|---|---|---|---|
| 1 | `AgentDefinition.hooks` | **忽略 + 诊断日志**，不拒绝建会话；不改共享 profile 对象 | §D 的 D3 行 |
| 2 | E2/E3 引擎侧兜底 | **必须兜底**——Chat/Code 共用连接，客户端实例级覆盖不是会话级边界；四源引擎内全归零，客户端四道检查保留为纵深防御 | §E 全节 |
| 3 | LSP 范围 | **Chat 全面禁用**（含用户/项目 `lsp.json`）——LSP 起后台进程且读工作区，与零文件面冲突，是否经插件不改变风险 | §A 全节 |

**语义收敛结论**：`local_extensions_disabled` 现覆盖 **plugins、所有 hooks、
所有非内建 MCP、所有 LSP、plugin skills/commands**——即
**禁用会话只能使用内建工具，所有外部扩展通道归零**。

**第三版补充（§I）**：该结论**同样适用于禁用会话派生的子代理**——
子代理严格继承策略位，且 `AgentDefinition` 自带的 hooks/MCP 与共享
plugin registry 三条来源一并归零。子代理不是"新会话"，不得成为
父会话隔离承诺的缺口。
