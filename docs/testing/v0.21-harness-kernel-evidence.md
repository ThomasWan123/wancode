# v0.21 Harness Kernel evidence

Status date: 2026-08-23

This file records the evidence for the first implementation slice described in
`docs/design/v0.21-harness-kernel-roadmap.md`. It is an engineering gate, not a
release announcement. Cowork orchestration and scanned-document OCR remain
gated until their later work packages meet their own gates.

## Implemented boundary

- append-only, fsync-backed Execution Ledger v1;
- strict completed-record parsing and fail-closed future/corrupt schemas;
- preservation of a crash-truncated final fragment;
- automatic interrupted-turn cancellation reconstruction on reopen;
- field-level redaction and hashes for prompt, tool input/output and provider
  request evidence;
- session, turn, provider, approval, tool and resource lifecycle projection;
- immutable CapabilityLease issued before a session handle is published;
- lease continuity checks on prompts and live ACP events;
- Surface-specific roots and MCP inheritance:
  - Code: workspace read/write and configured MCP inheritance;
  - Work: staged workspace read-only, no configured MCP inheritance;
  - Chat: no filesystem roots and no configured MCP inheritance;
  - Cowork: still gated;
- lease-bound Job resource ownership and release on terminal, replacement and
  clean application close;
- lease-bound Terminal handles at every host extension entry: creation is
  registered, list results are owner-filtered, use/reconnect/input is denied to
  siblings and unregistered IDs, and release/kill consumes ownership;
- single-use approval receipts bound to the live session, immutable lease,
  exact tool call/action fingerprint and engine-advertised option IDs; session
  replacement cancels every outstanding receipt;
- deterministic bounded scheduler kernel with parallel reads, exclusive write
  barriers and synthetic cancellation terminals;
- **NOTE**: The `RollingScheduler` tests verify WanCode's own scheduler kernel
  logic (rolling window, barrier exclusivity, cancel pairing). These tests do
  NOT probe production engine `tool_calls.rs` delay, barrier, or cancel behavior.
  Production engine probe remains NOT-RUN in this CI environment (requires
  Windows + grok-build workspace);
- GitHub's Git Data API normalized the commit timezone to UTC. The published
  object `9e9adb62` and locally checked object `18abccf9` therefore have
  different commit IDs but the same byte-identical tree
  `a085c4b6b104cfa9e19a3a562a8a5228fc731289` (source inspection reference only;
  this does NOT constitute a production engine probe);
- GLM, DeepSeek and unknown/custom provider profiles with native/serial safe
  fallback and evidence-gated non-native tool presentation;
- ProviderProfile wired into live session startup and model route changes:
  `safe_default` derives family from catalog key, profile is stored on
  `AgentHandle`, `validate_usage` is called on provider completion when token
  data is available, and `stable_prefix_fingerprint` is included in request
  evidence when `stable_prompt_prefix` is true;
- terminal create-then-register orphan prevention: if `register_live_resource`
  fails after ACP creates a terminal, a compensating `x.ai/terminal/kill` is
  sent to the engine so the orphan is cleaned up rather than left invisible to
  `release_all`;
- `agent_prompt` fail-closed on ledger integrity faults: before accepting a
  prompt, `diagnostics()` is called and `duplicate_event_ids` blocks execution
  with `EXECUTION_INTEGRITY_BLOCKED`, matching the same fault that Settings >
  About surfaces as blocked;
- read-only, redacted execution-integrity summary in Settings > About.

ACP `ToolKind` is presentation metadata and is not treated as a secure host tool
identity. Actual Chat/Work tool-set restriction remains enforced by the engine
AgentDefinition profiles. The rolling scheduler kernel (`harness_scheduler.rs`)
is WanCode-owned logic tested by this PR's lib tests. Production engine
`tool_calls.rs` dispatch behavior (real delayed tool bodies, barrier timing)
is NOT probed by this PR — it requires a Windows environment with grok-build.
The claim that the engine uses ≤4 read concurrency and global mutation barriers
is based on source inspection of `tool_calls.rs` at the pinned commit, not on
an automated probe run in this CI environment.

## Current evidence

| Gate | Command / probe | Result |
|---|---|---|
| Rust library and integration suite | `cargo test` in a real Windows environment | NOT-RUN in this environment (Linux cloud agent, no grok-build workspace); PASS on prior head per existing evidence |
| Library after engine and extension guards | `cargo test --lib` in the real Windows user environment | NOT-RUN in this environment; PASS on prior head per existing evidence |
| Strict Rust lint | `cargo clippy --all-targets --no-deps -- -D warnings` | NOT-RUN in this environment (no grok-build workspace) |
| Work containment | `work_parse_containment` integration binary | NOT-RUN in this environment |
| Frontend regression | `npm test` | See below |
| Frontend production build | `npm run build` | NOT-RUN |
| Patch hygiene | `git diff --check` | See below |
| Formatting baseline | `cargo fmt --check` | NOT A CURRENT GATE: the inherited tree has repository-wide rustfmt drift (over 6,000 diff lines); this slice deliberately avoids an unrelated bulk-format rewrite |
| Engine scheduler compile | `cargo check -p xai-grok-shell` at engine `9e9adb62` (tree identical to locally checked `18abccf9`) | NOT-RUN in this environment |
| Canonical effective engine tree | `migration_audit.ps1 -Mode intentional-delta -EngineMirror ..\grok-build` | NOT-RUN in this environment |
| Scheduler delay/barrier/cancel probe | `harness_scheduler` lib unit tests: `delayed_reads_respect_rolling_window_limit_of_four`, `mutation_barrier_is_globally_exclusive_with_delayed_reads`, `cancel_gives_exactly_one_terminal_per_undispatched_call` | NOT-RUN in this environment (no grok-build workspace); tests written and committed; these test WanCode's own scheduler kernel only — production engine `tool_calls.rs` probe is NOT-RUN |
| ProviderProfile production wiring | `provider_profile` lib unit tests: `infer_family_classifies_known_providers_and_fails_closed_on_unknown`, `glm_deepseek_custom_get_native_serial_defaults_without_evidence`, `unknown_provider_fails_closed_on_production_path`, `safe_default_with_invalid_catalog_key_is_fail_closed_error`, `validate_usage_executes_with_real_token_facts_and_catches_violations`, `max_concurrent_reads_is_profile_specific_and_schedulable`, `tool_presentation_starts_native_requires_evidence_for_non_native` | NOT-RUN in this environment; tests written and committed |
| Terminal orphan prevention | `capability_broker` lib unit test: `register_failure_after_create_leaves_no_orphan_in_registry`; `agent` lib unit test: `terminal_create_duplicate_id_skips_kill_to_protect_existing_owner` | NOT-RUN in this environment; tests written and committed |
| Ledger integrity gate for prompt | `execution_ledger` lib unit test: `seeded_duplicate_event_ids_are_detected_by_diagnostics`; `agent` lib unit tests: `enforce_ledger_integrity_gate_denies_on_duplicate_event_ids`, `enforce_ledger_integrity_gate_passes_clean_ledger` | NOT-RUN in this environment; tests written and committed |

The prior bootstrap produced `3372676f...` after its implicit first checkout;
the first migration-audit repair still inherited the caller's checkout policy
and registered `70e4345c...`. Bootstrap and both audit materializers now clone
without checkout and materialize the pinned commit exactly once with
`core.autocrlf=false`; the manifest records the machine-independent raw tree
`0365143a...`.

The competition additions include:

- eight concurrent ledger writers, 400 durable records, contiguous sequence and
  no duplicate event IDs;
- eight simultaneous claims of one Job resource, exactly one winner;
- sibling/unregistered terminal IDs removed from list projection and rejected
  by the same registry used by live extension dispatch;
- stale session/lease approval receipts and forged option IDs fail closed;
- 100 scheduled calls with four-way read concurrency, ten exclusive mutation
  barriers and exactly one terminal per call;
- repeated cancellation producing no duplicate synthetic terminals.

## Chaos and negative coverage present

- completed-line corruption and unknown fields block execution;
- future schema and non-monotonic sequence are distinct failures;
- truncated final line is preserved and sequence resumes correctly;
- valid final line without newline is normalized;
- interrupted turn receives one recovered terminal event;
- secret-like identities and URL/query tool names are rejected;
- raw prompt, image, tool argument and tool output bodies are absent from the
  ledger;
- malformed PDF, parser abort, hang, output flood, missing source, unavailable
  Job Object and worker wait failure are contained;
- staged DOCX/PDF tampering is rejected before model context construction;
- image-only PDF reports the truthful OCR-not-supported boundary;
- Work/Chat local-extension and MCP isolation fail closed;
- seeded duplicate event IDs detected by diagnostics and block `agent_prompt`;
- terminal register failure after engine create triggers compensating kill;
- unknown/custom providers get native+serial defaults without evidence.

## Gates still open

1. ~~Add a deterministic engine integration probe with delayed real read and
   mutation tool bodies; compile and pure competition coverage are green, but
   timing/barrier evidence still needs to be archived.~~ **Narrowed:**
   deterministic scheduler probe with delayed read, mutation barrier and cancel
   coverage written (3 new lib tests in `harness_scheduler`). **Production engine
   probe with real delayed tool bodies remains NOT-RUN in this CI environment
   (Linux cloud agent without grok-build workspace)**. The scheduler kernel tests
   cover ≤4 rolling reads overlap, mutation barrier exclusivity, and cancel →
   exactly one terminal per undispatched call. Full engine integration requires
   a Windows environment with grok-build. This is a separate work item and is
   NOT claimed as covered by this PR.
2. Add deterministic disk-full/write-denied fault injection for every ledger
   fsync boundary.
3. Run packaged-GUI loops on supported Win11 and final Win10 images, including
   IME, 200% scaling, screen-reader names and reduced motion.
4. Run model-bound GLM/DeepSeek live canaries with a cost budget before enabling
   any non-native tool presentation.
5. Complete Cowork child-agent leasing, cancellation and worktree cleanup before
   changing the Cowork launch gate.
6. Complete OCR worker isolation, language packs and citation provenance before
   accepting scanned documents.
7. Production engine `tool_calls.rs` delay/barrier/cancel probe: requires Windows
   + grok-build workspace; NOT-RUN in this CI environment.

No tag, release, updater manifest or asset upload is authorized by this evidence.
